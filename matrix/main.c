#include "matrix.h"


// uncomment out the functions which you wish to test
int main(void) {

    // matrix* m = create_matrix(5, 5);
    // matrix* m = zeros_matrix(m);
    // print_matrix(m);

    // int rows = get_rows(m);
    // int cols = get_cols(m);
    // printf("my matrix has %i rows and %i columns", rows, cols);

    // free_matrix(m);

    // matrix* m = zeros_matrix(create_matrix(3, 3));
    // print_matrix(m);
    // matrix* m = set_matrix(m, 2, 2, 1);
    // print_matrix(m);

}
